#include <stdio.h>
#include "main.h"

/**
 * _islower - checks if it is a lower case
 *
 * @ c: enable us to detict lowe case
 *
 * Return: 1 if lower case and zero if not
 */

int _islower(int c)
{
char c;
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
