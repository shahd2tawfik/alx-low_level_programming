#include <stdio.h>
#include "main.h"

/**
 * _isalpha - this is the master function
 *
 * @c: enable us to write a standard output
 *
 * Return: Always 0 (success)
 */

int _isalpha(int c)
{
if (c >= 'a' && c <= 'z')
|| (c >= 'A' && c <= 'Z')

{
return (1); 
}
else
{
return (0);
}
}
